# Member 3 trend agent

Add `trend_agent.py` here and expose `analyze_trends(readings) -> dict`. The result fills the unified `trends` object. Monitoring React components belong in `frontend/src/features/member3-monitoring`.

