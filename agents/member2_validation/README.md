# Member 2 validation agent

Add `validation_agent.py` here and expose `validate_readings(readings) -> dict`. The result fills the unified `quality` object. Dataset CSV files belong in `backend/datasets`.

